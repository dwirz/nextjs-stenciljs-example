{
  "name": "stenciljs-nextjs-example",
  "version": "0.0.0-development",
  "license": "ISC",
  "scripts": {
    "dev:app": "lerna run dev --skip-nx-cache --scope=abc-app",
    "build:web-components": "lerna run build --scope=abc-web-components"
  },
  "engines": {
    "node": "^20.0.0",
    "npm": "^10.0.0"
  },
  "devDependencies": {
    "lerna": "8.1.8"
  },
  "workspaces": [
    "packages/*"
  ]
}
